<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework parent to avoid pulling the Material XML library; the real
         theming happens in Compose (FuelRadarTheme). -->
    <style name="Theme.FuelRadar" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
